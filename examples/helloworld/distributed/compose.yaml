services:

    alpha:
        build: ./alpha

        restart: always

    beta:
        build: ./beta

        ports:
            - "3001:3001"

        restart: always

    gamma:
        build: ./gamma

        ports:
            - "3002:3002"

        restart: always