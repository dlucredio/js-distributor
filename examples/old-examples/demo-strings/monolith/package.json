{
  "name": "monolith",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "generate-single-http": "js-distributor-scripts --configFile config-http.yml --cleanOutput --generateProjects --generateDocker -i src -o ../distributed-http",
    "generate-single-rabbit": "js-distributor-scripts --configFile config-rabbit.yml --cleanOutput --generateProjects --generateDocker -i src -o ../distributed-rabbit"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "distributor": "file:../../../distributor"
  }  
}
